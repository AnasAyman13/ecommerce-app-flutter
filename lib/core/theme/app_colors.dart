import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Background and surfaces
  static const Color bgCream = Color(0xFFFAF6F0);
  static const Color white = Color(0xFFFFFFFF);
  static const Color cardBg = Color(0xFFFFFFFF);

  // Dark brown / charcoal
  static const Color darkBrown = Color(0xFF35211E);
  static const Color darkBrownPill = Color(0xFF35211E);

  // Maroon / Burgundy primary accents
  static const Color primaryMaroon = Color(0xFF832C31);
  static const Color maroonBadge = Color(0xFF832C31);

  // Light pill backgrounds
  static const Color lightPillBg = Color(0xFFF5EFE8);
  static const Color lightBorder = Color(0xFFECE4DC);

  // Text colors
  static const Color textDark = Color(0xFF35211E);
  static const Color textGrey = Color(0xFF8E8278);
  static const Color textLightGrey = Color(0xFFA1958C);

  // Status colors & chips
  // Delivered status (mint green)
  static const Color mintGreen = Color(0xFF2A9383);
  static const Color mintBg = Color(0xFFE6F5F2);

  // Out for Delivery status (amber/gold)
  static const Color amberGold = Color(0xFFB07D3E);
  static const Color amberBg = Color(0xFFF8F2E6);

  // Cancelled status (pink/red)
  static const Color cancelledRed = Color(0xFFC04245);
  static const Color cancelledBg = Color(0xFFFBEAEB);

  // Compatibility aliases
  static const Color backgroundLight = bgCream;
  static const Color background = bgCream;
  static const Color primary = primaryMaroon;
  static const Color header = primaryMaroon;
  static const Color gold = Color(0xFFBB8A52);
  static const Color starYellow = Color(0xFFFFBA00);
  static const Color black = Colors.black87;
  static const Color grey = Colors.grey;
}

